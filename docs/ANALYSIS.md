# 📊 การวิเคราะห์เปรียบเทียบ: VM vs Docker Deployment
## ENGSE207 - Week 6 N-Tier Architecture

**ชื่อ-นามสกุล:** [เจษฎา อินตา]  
**รหัสนักศึกษา:** [67543210006-2]  
**วันที่:** [---]

---

## 1. ตารางเปรียบเทียบ Setup Process

| ขั้นตอน            | Version 1 (VM)                                    | Version 2 (Docker)                               |
| ------------------ | ------------------------------------------------- | ------------------------------------------------ |
| ติดตั้ง PostgreSQL | ติดตั้งผ่าน apt/yum และตั้งค่า user, database เอง | ใช้ image `postgres:16-alpine` ใน docker-compose |
| ติดตั้ง Node.js    | ติดตั้ง Node.js และ npm เอง                       | Build จาก Dockerfile อัตโนมัติ                   |
| ติดตั้ง Nginx      | ติดตั้งผ่าน apt และแก้ config ด้วยตนเอง           | ใช้ image `nginx:alpine` และ mount config        |
| Configure Database | แก้ไฟล์ config และสร้าง DB manual                 | ใช้ environment variables และ init.sql           |
| Configure SSL      | สร้าง cert และตั้งค่าใน nginx เอง                 | Mount SSL เข้า container                         |
| Start Services     | ใช้ systemctl start ทีละ service                  | ใช้ `docker compose up -d` คำสั่งเดียว           |
| **เวลาทั้งหมด**    | ประมาณ 30–45 นาที                                 | ประมาณ 10–15 นาที                                |


---

## 2. ตารางเปรียบเทียบ Resource Usage

| Resource     | Version 1 (VM)                   | Version 2 (Docker)                |
| ------------ | -------------------------------- | --------------------------------- |
| Memory Usage | ใช้ RAM มากกว่า เพราะรันทั้ง OS  | ใช้ RAM น้อยกว่า เพราะแชร์ kernel |
| Disk Usage   | ใช้พื้นที่มาก (มี OS เต็มรูปแบบ) | ใช้พื้นที่เฉพาะ image และ volume  |
| CPU Usage    | มี overhead จาก hypervisor       | ใช้ CPU ใกล้เคียง native          |
| Startup Time | หลายสิบวินาที – หลายนาที         | ไม่กี่วินาที                      |


---

## 3. ข้อดีของ Docker Deployment (เขียน 5 ข้อ)

1. Setup เร็ว: ใช้ docker compose คำสั่งเดียวสามารถรันทั้งระบบได้

2. Environment Consistency: ทุกคนใช้ image เดียวกัน ลดปัญหา “เครื่องฉันใช้ได้”

3. แยก Service ชัดเจน: DB, API, Nginx แยก container ชัดเจนตาม N-tier

4. ง่ายต่อการ Deploy: สามารถย้ายไปรันเครื่องอื่นได้ทันที

5. จัดการง่าย: สามารถดู log, restart, scale ได้ง่ายด้วยคำสั่ง Docker

---

## 4. ข้อเสียของ Docker Deployment (เขียน 3 ข้อ)

1. ต้องเข้าใจ Docker ก่อน: มี learning curve สำหรับผู้เริ่มต้น

2. Debug ซับซ้อนขึ้น: ต้องตรวจ container, network และ healthcheck

3. Dependency กับ Docker Engine: ถ้า Docker มีปัญหา ระบบทั้งหมดจะรันไม่ได้

---

## 5. เมื่อไหร่ควรใช้ VM vs Docker?

### ควรใช้ VM เมื่อ:
- ต้องการจำลองทั้งระบบปฏิบัติการ

- ต้องการ isolation ระดับ OS เต็มรูปแบบ

- ใช้ระบบ legacy ที่ต้องพึ่ง OS เฉพาะ

### ควรใช้ Docker เมื่อ:
- พัฒนาเว็บแอปแบบ microservices

- ต้องการ deploy เร็วและทำ CI/CD

- ต้องการ environment ที่เหมือนกันทุกเครื่อง

---

## 6. สิ่งที่ได้เรียนรู้จาก Lab นี้

จาก Lab นี้ได้เรียนรู้การออกแบบระบบแบบ N-Tier Architecture โดยแยก Database, Backend และ Web Server ออกจากกันอย่างชัดเจน นอกจากนี้ยังเข้าใจการใช้ Docker Compose ในการจัดการหลาย services พร้อมกัน รวมถึงการตั้งค่า healthcheck และ reverse proxy ด้วย Nginx ทำให้เห็นความแตกต่างระหว่างการ deploy แบบ VM และแบบ Container 

---

## 7. คำสั่ง Docker ที่ใช้บ่อย (Quick Reference)

```bash
docker compose up -d
docker compose down
docker compose logs -f
docker ps
docker build -t image-name .
docker exec -it container-name sh
docker stats
docker inspect container-name
```
